//    DATA_TYPES

// "use strict" //treat js code as newer version

let name="ella" //string
let age=22      //number,   bigint => for larger numbers
let isApproved=true //boolean

// null => standalone value -> represents an empty value  { also a OBLECT }
// undefined => variable that has not been assigned a value yet like accountState in lec1.js, { undefined is a type itself }
// symbol => used to create unique identifiers for objects


console.log(typeof "ella");  //typeof operator is used to check the data type of a variable (kis type ka hai)

